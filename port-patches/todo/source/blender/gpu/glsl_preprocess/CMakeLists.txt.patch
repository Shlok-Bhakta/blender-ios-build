diff --git a/source/blender/gpu/glsl_preprocess/CMakeLists.txt b/source/blender/gpu/glsl_preprocess/CMakeLists.txt
index e33f39c4630..9561a631754 100644
--- a/source/blender/gpu/glsl_preprocess/CMakeLists.txt
+++ b/source/blender/gpu/glsl_preprocess/CMakeLists.txt
@@ -2,20 +2,22 @@
 #
 # SPDX-License-Identifier: GPL-2.0-or-later
 
+# -------------- IPAD CROSS COMPILE TOOLS ----------- #
+if(NOT WITH_CROSSCOMPILED_TOOLS)
 
-# -----------------------------------------------------------------------------
-# Build `glsl_preprocess` executable.
-set(SRC
-  glsl_preprocess.cc
+  # -----------------------------------------------------------------------------
+  # Build `glsl_preprocess` executable.
+  set(SRC
+    glsl_preprocess.cc
 
-  glsl_preprocess.hh
-  shader_parser.hh
+    glsl_preprocess.hh
+    shader_parser.hh
 )
 
-if(WITH_GPU_SHADER_ASSERT)
-  add_definitions(-DWITH_GPU_SHADER_ASSERT)
-endif()
+  if(WITH_GPU_SHADER_ASSERT)
+    add_definitions(-DWITH_GPU_SHADER_ASSERT)
+  endif()
 
-# `SRC_DNA_INC` is defined in the parent directory.
-add_executable(glsl_preprocess ${SRC})
-optimize_debug_target(glsl_preprocess)
+  # `SRC_DNA_INC` is defined in the parent directory.
+  add_executable(glsl_preprocess ${SRC})
+endif()
